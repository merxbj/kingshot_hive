services:
  kingshot:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DB_PATH=/data/kingshot.db
      - CORS_ORIGIN=
    restart: unless-stopped
